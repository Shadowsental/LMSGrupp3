function getContrast(hexcolor) {

let r = parseInt(haxcolor.substr(0,2), 16);

let g = parseInt(hexcolor.substr(2,2), 16);

let b = parseInt(hexcolor.substr(4,2), 16);

let magicNumber = ((r*299)+(g*587)+(b*114))/1000;

return (magicNumber >= 128) ? 'black' : 'white';

}